{
  "name": "courtflow",
  "version": "0.1.0",
  "private": true,
  "scripts": { "dev": "node server.mjs", "start": "node server.mjs", "build": "node build.mjs", "check": "node --check app.js && node --check server.mjs && node --check build.mjs" }
}
